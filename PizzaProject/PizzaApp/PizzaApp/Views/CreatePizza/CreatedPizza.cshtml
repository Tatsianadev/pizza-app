@model PizzaApp.Models.OrderViewModel
@{
    ViewBag.Title = "CreatedPizza";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>CreatedPizza</h2>

@*<div class="container text-center create-image">
    <div class="mx-auto pt-2 pb-2 bg-dark col-4 bg-success p-2 text-white contur bg-opacity-75" style="margin: 30px 0 20px">
           
        <form method="post" action="/CreatedPizza/Index">
            <table width="100%">
                <tr>
                    <td align="right" valign="top" width="100" class="text-white contur" style="font-weight: normal; font-family: cursive">
                        Pizza name:
                    </td>
                    <td align="left" valign="top" width="100"  style="padding:10px 10px; color:black">
                        <input type="text" name="PizzaName" value="@Model.PizzaName" />
                        <div id="name">@Model.PizzaName</div>

                    </td>
                </tr>

                <tr>
                    <td align="right" class="text-white contur" style="font-weight: normal; font-family: cursive">
                        Size:
                    </td>
                    <td align="left" width="100" style="padding: 10px 10px; color: black">
                        <input type="checkbox" name="Size" value="@Model.Size" checked />
                        @Model.Size
                        <div id="size">@Model.Size</div>

                    </td>
                </tr>

                <tr>
                    <td align="right" valign="top" class="text-white contur" style="font-weight: normal; font-family: cursive">
                        Ingredients:
                    </td>
                    <td align="left" valign="top" width="100" style="padding:10px 10px">

                        @foreach (var ingredient in Model.Ingredients)
                        {
                            <input type="checkbox" name="Ingredients" value="@ingredient.Name" checked />
                            @ingredient.Name;
                            @ingredient.Name;
                            <br />
                        }
                    </td>
                </tr>
                <tr>
                    <td align="right" class="text-white contur" style="font-weight: normal">
                        Final price:
                    </td>
                    <td align="left" width="100" style="padding:10px 10px">
                        <div>
                            <div id="price" style="display:inline-block"> @Model.PizzaPrice</div>
                            <div style="display:inline-block">$</div>
                        </div>

                    </td>
                </tr>


            </table>
            <br />
            <br />
            <div class="text-center">
                What the awesome Pizza!
                <br />
                You're cool chef!
            </div>

            @if (User.Identity.IsAuthenticated)
            {
                <button type="submit" class="btn btn-secondary mt-2">Add pizza to order</button>
                
                <a href="@Url.Action("CreatePizza","CreatePizza")" class="btn btn-secondary mt-2">Cancel</a>
            }
            else
            {
                <button class="btn btn-secondary mt-2">Add pizza to order</button>
              
                <a href="@Url.Action("CreatePizza","CreatePizza")" class="btn btn-secondary mt-2">Cancel</a>
            }

        </form>
       


    </div>
    

</div>*@

<div class="container text-center create-image">
    <div class="mx-auto pt-2 pb-2 bg-dark col-4 bg-success p-2 text-white contur bg-opacity-75" style="margin: 30px 0 20px">
        <table width="100%">
            <tr>
                <td align="right" valign="top" width="100" class="text-white contur" style="font-weight: normal; font-family: cursive">
                    Pizza name:
                </td>
                <td align="left" valign="top" width="100" class="text-white" style="padding:10px 10px">
                    <div id="name"> @Model.PizzaName</div>

                </td>
            </tr>

            <tr>
                <td align="right" class="text-white contur" style="font-weight: normal; font-family: cursive">
                    Size:
                </td>
                <td align="left" width="100" style="padding:10px 10px">
                    <div id="size">@Model.Size</div>

                </td>
            </tr>

            <tr>
                <td align="right" valign="top" class="text-white contur" style="font-weight: normal; font-family: cursive">
                    Ingredients:
                </td>
                <td align="left" valign="top" width="100" style="padding:10px 10px">
                    @foreach (var ingredient in Model.Ingredients)
                    {
                    <div id="ingredient">@ingredient.Name</div>
                        @*@ingredient.Name;*@
                        <br />
                    }
                </td>
            </tr>
            <tr>
                <td align="right" class="text-white contur" style="font-weight: normal">
                    Final price:
                </td>
                <td align="left" width="100" style="padding:10px 10px">
                    <div>
                        <div id="price" style="display:inline-block"> @Model.PizzaPrice</div>
                        <div style="display:inline-block">$</div>
                    </div>

                </td>
            </tr>


        </table>
        <br />
        <br />
        <div class="text-center">
            What the awesome Pizza!
            <br />
            You're cool chef!
        </div>
        <form>
            @if (User.Identity.IsAuthenticated)
            {
                <a class="btn btn-secondary mt-2" onclick="AddCreatedPizzaToOrder('@Model.CustomPizzaId', '@User.Identity.Name')">Add pizza to order</a>

                <a href="@Url.Action("CreatePizza","CreatePizza")" class="btn btn-secondary mt-2">Cancel</a>
            }
            else
            {
                <button class="btn btn-secondary mt-2">Add pizza to order</button>
               
                <a href="@Url.Action("CreatePizza","CreatePizza")" class="btn btn-secondary mt-2">Cancel</a>
            }

        </form>

    </div>


</div>

